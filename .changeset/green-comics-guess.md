---
"@shiftbloom-studio/circadian-ui": patch
---

Refresh the build and CI toolchain to the latest stable releases and add the minimal TypeScript 6 migration settings required for tests and declaration builds.
